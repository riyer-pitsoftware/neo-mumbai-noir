# /noir-extract-characters

Extract character descriptions from story.txt using Ollama.

## Steps
1. Run `python extract_characters.py`
2. Review output in `characters/character_data.json`
3. Run `python verify_output.py` to validate

## Output
- `characters/character_data.json` — structured character data
- `characters/search_queries.json` — image search queries
